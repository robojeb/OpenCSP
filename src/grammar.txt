
File => many(Def; | Exec;)

Def => VarDef | ProgDef

ParamName => (Name | Name<list(Expression)>)

VarDef => ParamName := Expr;

ProgDef => ParamName :== [many(Operation;)]; //Param name expressions must all be ParamName type

